# Standard DSH bundle layer. The profile loader applies this after the web
# bundle and before the user's own ~/.dsh/profiles/web/cordis.patch.yml.
- insert:
    - id: balance-host
      name: '@deepseek-ai/dsh-host-balance'
    - id: balance-client
      name: '@deepseek-ai/dsh-client-balance'